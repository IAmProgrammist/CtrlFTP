package rchat.info.ctrlftp.core.dependencies;

public class DependencyInjector {
    /**
     * A utility method to inject dependency. Uses dependency manager as a
     * store of dependencies. Accepts a class and constructs it
     */
    private static void inject() {

    }
}
